## Run the following commands for backend:

npm install express multer csv-parser cors child_process

python3 -m venv venv
source venv/bin/activate

pip install -r requirements.txt

pip list

node index.js